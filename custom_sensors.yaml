door_living_room:
  friendly_name: "Living Room Door"
  value_template: >-
    {%- if is_state("sensor.sensative_strips_access_control_2_9", "22") -%}
      open
    {%- else -%}
      closed
    {%- endif %}

living_room_is_dim:
  friendly_name: "is the living room light dim"
  value_template: >-
    {%- if is_state("light.main_light", "off") or states.light.main_light.attributes.brightness <= 100 -%}
      yes
    {%- else  -%}
      no
    {%- endif %}
    
bathroom_motion:
  value_template: >-
    {%- if is_state("sensor.aeotec_zw100_multisensor_6_burglar_3_10", "8") -%}
      yes
    {%- else  -%}
      no
    {%- endif %}
